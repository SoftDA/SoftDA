import flow.des

module Signoff(
  in,
  out
);

input depedency in [3];
output depedency out [3];

Flow f1(.in(in[0]), .out(out[0]));
Flow f2(.in(in[1]), .out(out[1]));
Flow f3(.in(in[2]), .out(out[2]));

endmodule
