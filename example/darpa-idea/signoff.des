import flow.des

module Signoff(
  in1, in2, in3
  out1, out2, out3
);

input in1;
input in2;
input in3;
output out1;
output out2;
output out3;

Flow f1(.in(in1), .out(out1));
Flow f2(.in(in2), .out(out2));
Flow f3(.in(in3), .out(out3));

endmodule
